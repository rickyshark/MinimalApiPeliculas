name: Haciendo prueba en el codigo

on:
    push:
        branches:
        - HacerMejorasAqui

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v2

        - name: Run tests
          run: echo "Testeo de codigo"